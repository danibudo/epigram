services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: epigrams
      POSTGRES_USER: epigram_user
      POSTGRES_PASSWORD: epigram_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: